import java.awt.*;
import java.swing.*;
public class Curriculum extends JFrame{
	public Curriculum(){
		setLayout(new BoxLayout(this, BoxLayout.Y_Axis));
		JButton b1=new JButton("NOTES");
		JButton b1=new JButton("NOTES");
		JButton b1=new JButton("NOTES");
		add(createVerticalGlue);
add(b1);
add(b2);
add(b3);




	}
}